#include "main.h"

/**
 * main - Prints the programs executable name
 * @argc: The number of arguments passed
 * @argv: The array pointer to a string
 *
 * Return: Always 0
 */
int main(__attribute__((unused))int argc, char **argv)
{
	printf("%s\n", argv[0]);

	return (0);
}
